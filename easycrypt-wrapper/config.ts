const TIMEOUT_IN_SECONDS = 60;
export const PRINT_ALL_ERRORS = false;
export const TERMINAL_MODE = true; // true orders things best for a terminal where focus pins to the last output,
                                   // and false for a file where focus pins to the top

export const TIMEOUT = TIMEOUT_IN_SECONDS * 1000;